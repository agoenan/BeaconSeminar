angular.module('BeaconApp.informationControllers', [])

//InformationController
.controller("InformationCtrl", function($scope, $rootScope, $ionicPlatform) {

    
  
       
   //   var pageId = pluginResult.region.identifier

	//console.log('ranged beacon: ' + pageId + ' ' + beacon.proximity)

	// If the beacon is close and represents a new page, then show the page.
//	if ((beacon.proximity == 'ProximityImmediate' || beacon.proximity == 'ProximityNear')
//		&& app.currentPage != pageId)
//{
//		app.gotoPage(pageId)
//		return
//	}

	// If the beacon represents the current page but is far away,
// then show the default page.
//	if ((beacon.proximity == 'ProximityFar' || beacon.proximity == 'ProximityUnknown')
//		&& app.currentPage == pageId)
//	{
//		app.gotoPage('page-default')
//		return
//	}
//}
       
        
});
       
        